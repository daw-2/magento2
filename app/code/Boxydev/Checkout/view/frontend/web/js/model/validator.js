define([
    'Magento_Ui/js/model/messageList'
], function (messageList) {
    'use strict';

    return {
        validate: function () {
            // messageList.addErrorMessage({message: 'Erreur'});

            return true;
        }
    }
});
